0. run "npm i" in the current directory (node.js and npm required) (OR use install.bat on the windows)
1. Drop JSON key files into /keys folder
2. Insert URL strings into urls.txt
3. Set config in the index.js (if is needed)
4. Run "node index.js"  in the current directory (OR use !!!run.bat on the windows)